language: r
sudo: false
cache: packages

warnings_are_errors: true

compiler: clang

addons:
  apt:
    packages:
      - libgsl0-dev
 
r_github_packages:
 - jimhester/covr
 
after_success:
 - Rscript -e 'library(covr);codecov()'
 
r_check_args: "--as-cran"

